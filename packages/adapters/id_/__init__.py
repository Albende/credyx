"""Indonesia (ID) country adapter package.

The folder is named ``id_`` rather than ``id`` to avoid shadowing the
Python built-in ``id()``. The country code itself is still ``ID``.
"""
from packages.adapters.id_.adapter import IDAdapter

__all__ = ["IDAdapter"]
