"""Iceland (IS) country adapter package.

The folder is named ``is_`` rather than ``is`` to avoid clashing with the
Python ``is`` reserved keyword. The country code itself is still ``IS``.
"""
from packages.adapters.is_.adapter import ISAdapter

__all__ = ["ISAdapter"]
